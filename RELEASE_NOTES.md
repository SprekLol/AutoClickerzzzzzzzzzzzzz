# v1.0.5

输入体验修复：

- 移除主页面长期保留的大块底部空白，恢复更自然的页面尾部间距。
- 键盘弹出时通过 IME Insets 动态增加可滚动空间，键盘收起后自动恢复。
- 对部分系统 IME Insets 不稳定的情况增加可见窗口高度兜底检测。
- 输入框获得焦点后会多次校正滚动位置，确保底部设置项不会停在输入法遮挡区域。

APK 文件：`lian-dian-qi-v1.0.5-release.apk`

# v1.0.4

关键修复与展示更新：

- 修正选区层坐标与辅助功能点击坐标不一致导致的点击偏移。
- 选区保存时统一使用真实屏幕坐标，显示、设置和实际点击位置保持一致。
- 主界面输入框获得焦点后会自动滚动，避免底部悬浮窗设置被键盘遮挡。
- 悬浮球改为真正的圆形。
- 区域随机点击改用更不可预测的随机源；矩形范围内均匀取点，圆形范围内按面积均匀取点。
- README 加入四张界面示意图，并保留中文/英文两个独立页面。

APK 文件：`lian-dian-qi-v1.0.4-release.apk`

# v1.0.3

UI 细节修正：

- 将上一版手写连续圆角替换为 superellipse/squircle 曲线，更接近 iOS/Figma 的平滑圆角。

# v1.0.2

交互与逻辑改进：

- 开关关闭时，对应设置项会置灰并禁止填写。
- 关闭区域随机点击后，可单独设置固定点击点。
- 悬浮窗支持透明度设置，并会在无操作后自动收起成悬浮球。
- 选区层在随机模式下选范围，在非随机模式下选固定点。
- 主界面和权限页为状态栏留出安全距离。
- 卡片、按钮、输入框改用连续平滑圆角。
- 验证随机矩形和圆形点击点不会超出选中范围。

# v1.0.1

界面小改版：

- 主界面改成分组卡片和圆角输入框，修正数字输入挤在下划线上的问题。
- 使用类似 Material You 的动态主题色，优先跟随系统/壁纸取色。
- 权限入口移动到独立页面，主界面按钮层级更清楚。
- 悬浮控制面板和选区按钮统一为更柔和的圆角样式。
- 辅助功能状态判断更准确。

# v1.0.0

首个版本：

- 支持固定次数和无限点击。
- 支持点击间隔设置。
- 支持矩形范围和中心点半径范围。
- 支持范围内随机点击。
- 支持随机时间浮动。
- 支持悬浮控制面板和全屏范围选择层。
- 使用 Android 辅助功能服务执行点击。
