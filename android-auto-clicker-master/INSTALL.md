# 安装说明

APK 文件在项目根目录：

```text
lian-dian-qi-v1.0.3-release.apk
```

安装后第一次打开：

1. 点“开启悬浮窗权限”，允许连点器显示在其他应用上层。
2. 点“开启辅助功能权限”，找到并启用“连点器点击服务”。
3. 回到连点器设置点击次数、间隔和范围。
4. 点“打开悬浮控制”，切到目标应用后点“开始”。

如果手机提示“未知来源应用”，需要在系统安装提示里允许本次安装。
